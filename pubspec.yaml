name: checklist_app
description: A new Flutter project

publish_to: 'none' # Remove this line if publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Tus dependencias van todas aquí, debajo de flutter:
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/images/imagen1.png
    - assets/images/imagen2.png
    - assets/images/imagen3.png
    - assets/images/footer.png
